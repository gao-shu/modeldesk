# {Vendor} — `{api_format.id}`

> 对照表：官方能力 ↔ ModelDesk 已接能力。勿大段复制厂商文档正文。

## Meta

| 项 | 值 |
|----|-----|
| `api_format` | `{id}` |
| Modality | `text` / `image` / `video` / `audio` / `music` |
| Tier | `core` / `extended` / `relay` |
| 建议 Base URL | |
| 典型 Model ID | |
| 代码入口 | `packages/shared/src/api-formats.ts` · `packages/adapters/src/…` |
| 适配度 | 待校验 / 对齐 / 部分对齐 |
| 上次校验 | YYYY-MM-DD |

## 官方文档

| 说明 | URL |
|------|-----|
| 主文档 | （待补） |
| 其它 | |

## 能力矩阵

| 能力 | 文档 | 本项目 | 备注 |
|------|------|--------|------|
| … | ✓/✗ | ✓/✗/— | |

说明：`—` = 故意不做；`部分` = UI 或参数未完全对齐。

## UI 参数与约束

- 来源：`ApiFormatDef.fields`（及按 model 分化的逻辑，如有）
- 列出与文档不一致或易踩坑的枚举（分辨率、时长、quality 等）

## 已知坑

-

## 校验记录

| 日期 | 结论 | 变更 |
|------|------|------|
| YYYY-MM-DD | 档案初建 | |
